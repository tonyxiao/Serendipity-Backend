Meteor.methods({
  newMatch: function() {
    /*
    console.log("new matching");
    var randomUser = randomFromCollection(Meteor.users)();
    console.log(randomUser);
    Meteor.users.update({_id: this.userId}, {
      $set: {
        "current_match": randomUser
      }
    });
    */
  }
});