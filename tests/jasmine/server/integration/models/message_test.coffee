SimpleSchema = () ->
  attachSchema = () ->
    return

Users.attachBehaviour = () ->
  return

Connections.attachBehaviour = () ->
  return

Messages.attachBehaviour = () ->
  return

Candidates.attachBehaviour = () ->
  return

describe 'Message model', () ->
  it 'should find a connection', () ->
    expect(true).toBe(true)